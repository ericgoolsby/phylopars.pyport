% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradient.R
\name{evo_param_gradient}
\alias{evo_param_gradient}
\title{Compute Gradient of Log-Likelihood w.r.t. Evolutionary Parameter}
\usage{
evo_param_gradient(b, R, tree, model, evo_par)
}
\arguments{
\item{b}{Result from bastide_traversal() (must include backward pass quantities)}

\item{R}{Phylogenetic rate matrix (p x p)}

\item{tree}{The original (pre-transform) phylo object with root_dist computed}

\item{model}{Character string: evolutionary model}

\item{evo_par}{Current evolutionary parameter value}
}
\value{
Scalar gradient dlogL/d(evo_param)
}
\description{
Uses the chain rule: dlogL/d(evo_param) = sum_k (dlogL/dt'_k) * (dt'_k/d(evo_param))
}
\details{
The per-edge dlogL/dt'_k is computed from the backward pass quantities:
  A_k = Q_k %*% (Q_k^{-1} - (M_k - qn_k)(M_k - qn_k)' - V_k) %*% Q_k
  dlogL/dt'_k = -0.5 * tr(A_k %*% R)

Only tree edges contribute (not observation edges or the fake root edge).
}
\keyword{internal}
