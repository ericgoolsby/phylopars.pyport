% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylopars.R
\name{phylopars}
\alias{phylopars}
\title{Fit a phylogenetic mixed model using REML}
\usage{
phylopars(
  Y,
  tree,
  Sigma_start = NULL,
  B_start = NULL,
  Sigma_fixed = NULL,
  B_fixed = NULL,
  Sigma_diag = FALSE,
  B_diag = FALSE,
  method = "BFGS",
  control = list(),
  model = "BM",
  evo_model_par_start = NULL,
  evo_model_par_fixed = NULL,
  backend = c("armadillo", "R", "eigen")
)
}
\arguments{
\item{Y}{Data frame with species names in first column, trait values in
remaining columns. Missing values (NA) are allowed.}

\item{tree}{A phylo object (from ape package)}

\item{Sigma_start}{Starting value for Sigma matrix (default: identity)}

\item{B_start}{Starting value for B matrix (default: identity)}

\item{Sigma_fixed}{Fixed value for Sigma (not optimized if provided)}

\item{B_fixed}{Fixed value for B (not optimized if provided)}

\item{Sigma_diag}{If TRUE, constrain Sigma to be diagonal}

\item{B_diag}{If TRUE, constrain B to be diagonal}

\item{method}{Optimization method for optim() (default: "BFGS")}

\item{control}{List of control parameters for optim()}

\item{model}{Evolutionary model: "BM", "lambda", "kappa", "delta", "EB", or "OU"}

\item{evo_model_par_start}{Starting value for evolutionary model parameter}

\item{evo_model_par_fixed}{Fixed value for evolutionary model parameter}
}
\value{
List containing:
  \item{mu}{Estimated mean vector}
  \item{Sigma}{Estimated phylogenetic covariance matrix}
  \item{B}{Estimated residual covariance matrix}
  \item{evo_model_par}{Estimated evolutionary model parameter (if applicable)}
  \item{logL}{Restricted log-likelihood at optimum}
  \item{AIC}{Akaike Information Criterion}
  \item{convergence}{Convergence information from optim()}
  \item{model}{Evolutionary model used}
  \item{npars}{Number of parameters estimated}
}
\description{
Estimates phylogenetic (Sigma) and residual (B) covariance matrices using
restricted maximum likelihood. Handles missing data and within-species
variation (multiple observations per species).
}
\examples{
\dontrun{
library(ape)

# Simulate a tree
tree <- rtree(20)

# Create trait data with some missing values
Y <- data.frame(
  species = tree$tip.label,
  trait1 = rnorm(20),
  trait2 = rnorm(20)
)
Y$trait1[c(3, 7)] <- NA

# Fit model
result <- phylopars(Y, tree)
print(result$Sigma)
print(result$B)
}

}
